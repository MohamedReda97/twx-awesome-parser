# Extractable components

No framework components are available for extraction. The existing sidebar, top bar, cards, badges, and disclosure groups are inline HTML/CSS/JavaScript patterns rather than standalone components. For the new presentation page, reuse their tokens and visual language directly instead of introducing a component framework.
