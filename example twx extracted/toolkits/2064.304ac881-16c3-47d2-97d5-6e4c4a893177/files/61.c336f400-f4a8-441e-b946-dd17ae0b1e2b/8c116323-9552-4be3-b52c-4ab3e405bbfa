/*
 * #BEGIN COPYRIGHT
 *
 * Licensed Materials - Property of IBM
 * 5725-C95
 * (C) Copyright IBM Corporation 2017. All Rights Reserved.
 * US Government Users Restricted Rights- Use, duplication or disclosure
 * restricted by GSA ADP Schedule Contract with IBM Corp.
 *
 * #END COPYRIGHT
 */

/**
 * @ignore
 * @instance
 * @memberof TableLayout
 * @method isBound
 */  

/**
 * @ignore
 * @instance
 * @memberof TableLayout
 * @method isValid
 */  

/**
 * @ignore
 * @instance
 * @memberof TableLayout
 * @method setValid
 */

/**
 * @ignore
 * @instance
 * @memberof TableLayout
 * @method recalculate
 */

/**
 * @ignore
 * @instance
 * @memberof TableLayout
 * @method getData
 */

/**
 * @ignore
 * @instance
 * @memberof TableLayout
 * @method setData
 */
 
/**
 * </table>
 */
 
bpmext_control_InitTableLayout = function (bpmext, utilities, domClass, domAttr, domConstruct)
{
	this._instance = {
		
	}

	if(!this.constructor.prototype._proto)
	{
		this.constructor.prototype.IS_ADDRESS_INVISIBLE = true;
		
		this.constructor.prototype._proto =
		{
			BCOL_STL_MAP :
			{
				"S" : "separate",
				"C" : "collapse",
				"H" : "inherit"
			},
			_normalizeBorderCollapse : function(val)
			{
				if(val == null || val == "")
					val = "H";
				
				val = val.toUpperCase();
				switch(val)
				{
					case "H":
					case "C":
					case "S":
						break;
					default:
					{
						if(bpmext.text.startsWith(val, "SEP"))
							val = "S";
						else if(bpmext.text.startsWith(val, "COL"))
							val = "C";
						else
							val = "H";
					}
				}
			
				return val;
			},
			_getContentBox : function(view)
			{
				if(!view._instance.contentBox)
				{
					var children = view.context.element.children;
					for(var i = 0; i < children.length; i++)
					{
						var child = children[i];
						if(child.className && /\bContentBox\b/.test(child.className))
						{
							view._instance.contentBox = child;
							break;
						}
					}				
				}
					
				return view._instance.contentBox;
			},
			_replaceTagName : function(domElement, newTagName)
			{
				var res = document.createElement(newTagName);
				var children = domElement.children;
				
				for(var i = 0; i < children.length; i++)
				{
					res.appendChild(children[i].cloneNode(true));
				}
				
				for(var i = 0; i < domElement.attributes.length; i++)
				{
					var nodeName = domElement.attributes.item(i).nodeName;
					var nodeValue = domElement.attributes.item(i).value;
					
					res.setAttribute(nodeName, nodeValue);
				}
				
				return res;
			},
			_updateTableStyle : function(view)
			{
				var bsp = view.context.options.borderSpacing.get("value");
				var bcol = this._normalizeBorderCollapse(view.context.options.borderCollapse.get("value"));
				if (bsp != null && bsp.length > 0)
				{
					if (!isNaN(bsp))
						bsp += "px";
				}
				
				view._instance.table.style.borderSpacing = bsp;
				view._instance.table.style.borderCollapse = this.BCOL_STL_MAP[bcol];
			}
		};

		/**
		 * @instance
		 * @memberof TableLayout
		 * @method getType
		 * @desc Get descriptive string representing the type of control
		 * @returns {string}
		 */
		this.constructor.prototype.getType = function ()
		{
			return "tableLayout.3";
		}
		
		this.constructor.prototype.load = function ()
		{
			try
			{
				var opts = this.context.options;
				var mdt = opts._metadata;

				if (!opts.borderSpacing)
					bpmext.ui.substituteConfigOption(this, "borderSpacing", "inherit");
				
				if (!opts.borderCollapse)
					bpmext.ui.substituteConfigOption(this, "borderCollapse", "S");

				var cBox = this._proto._getContentBox(this);
				var cs = cBox.children;
				this._instance.table = document.createElement("table");
				this._proto._updateTableStyle(this);
				
				for(var i = 0; i < cs.length; i++)
				{
					var child = cs[i];
				   
					if(domAttr.has(child, "data-viewid"))
					{
						var child2 = this._proto._replaceTagName(child, "tr");
						this._instance.table.appendChild(child2);
						this.context.createView(child2, null, this);
					}
					
					domClass.add(child2, "tableLayoutRow");
				}
				
				this.context.element.appendChild(this._instance.table);
				cBox.parentNode.removeChild(cBox);
				
				bpmext.ui.loadContainer(this);
			}
			catch (e)
			{
				//{#feature: US-1330 Added RT localization}
				//bpmext.log.error("Error on load event [" + this.context.viewid + "]: " + e);
				bpmext.log.error(bpmext.localization.formatMsg("general", "ERROR_ONLOAD_EVENT") + " [" + this.context.viewid + "]: " + e);
				if(e.stack) { 
					//{#feature: US-1330 Added RT localization}
					//bpmext.log.error("  Call stack: " + e.stack);
					bpmext.log.error("  " + bpmext.localization.formatMsg("general", "CALL_STACK", e.stack));
				}
			}
		};
		
		this.constructor.prototype.unload = function ()
		{
			bpmext.ui.unloadContainer(this);
		};

		this.constructor.prototype.view = function ()
		{
			try
			{
				var visibility = utilities.handleVisibility(this.context);
			}
			catch (e)
			{
				//{#feature: US-1330 Added RT localization}
				//bpmext.log.error("Error on view event [" + this.ui.getAbsoluteName() + "]: " + e);
				bpmext.log.error(bpmext.localization.formatMsg("general", "ERROR_ONVIEW_EVENT") + " [" + this.ui.getAbsoluteName() + "]: " + e);
				if(e.stack) { 
					//{#feature: US-1330 Added RT localization}
					//bpmext.log.error("  Call stack: " + e.stack);
					bpmext.log.error("  " + bpmext.localization.formatMsg("general", "CALL_STACK", e.stack));
				}
			}
		};
		
		this.constructor.prototype.change = function (event)
		{
			try
			{
				if (event.type == "config")
				{
					switch (event.property)
					{
						case "_metadata.visibility":
						{
							this.view();
							break;
						}
						case "borderSpacing":
						case "borderCollapse":
						{
							this._proto._updateTableStyle(this);
							break;
						}
					}
				}
			}
			catch (e)
			{
				//{#feature: US-1330 Added RT localization}
				//bpmext.log.error("Error on change event [" + this.ui.getAbsoluteName() + "]: " + e);
				bpmext.log.error(bpmext.localization.formatMsg("general", "ERROR_ONCHANGE_EVENT") + " [" + this.ui.getAbsoluteName() + "]: " + e);
				if(e.stack) { 
					//{#feature: US-1330 Added RT localization}
					//bpmext.log.error("  Call stack: " + e.stack);
					bpmext.log.error("  " + bpmext.localization.formatMsg("general", "CALL_STACK", e.stack));
				}
			}        
		}
	}
}
